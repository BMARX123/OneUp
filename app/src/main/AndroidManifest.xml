<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lowercasewon.oneup">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FinancialOverview"
            android:label="@string/title_activity_financial_overview">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MonthlyStatements"
            android:label="@string/title_activity_monthly_statements" />
        <activity
            android:name=".DebtTracker"
            android:label="@string/title_activity_debt_tracker" />
        <activity
            android:name=".InvestmentPortfolio"
            android:label="@string/title_activity_investment_portfolio" />
        <activity
            android:name=".AddEvent"
            android:label="@string/title_activity_monthly_averages" />
        <activity
            android:name=".SettingsPage"
            android:label="@string/title_activity_settings_page" />
        <activity
            android:name=".CurrentDebts"
            android:label="@string/title_activity_current_debts" />
        <activity
            android:name=".CurrentInvestments"
            android:label="@string/title_activity_current_investments" />
        <activity
            android:name=".SpendingHabits"
            android:label="@string/title_activity_spending_habits" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".Q1" />
        <activity android:name=".Q2" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".BankATM"
            android:label="@string/title_activity_bank_atm" />
        <activity android:name=".Q3" />
        <activity android:name=".Q4" />
        <activity android:name=".Q5" />
        <activity android:name=".Q6"></activity>
    </application>

</manifest>